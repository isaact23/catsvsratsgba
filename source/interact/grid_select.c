#include "interact/grid_select.h"

struct sprite* select_cursor;

// Initialize grid selector
void grid_selector_init() {
    select_cursor = sprite_manager_new_sprite();
}

// Update grid selector every frame
void grid_select_update() {
    
}
